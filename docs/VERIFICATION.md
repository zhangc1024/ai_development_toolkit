# 本次验证记录

日期：2026-09-16（北京时间）。

## 自动检查

- Node.js 22.22.2 / npm 10.9.7（WSL Ubuntu）。
- `npm ci`：通过，锁文件可重现安装。
- `npm test`：29 项通过，涵盖合法根值、非法 JSON、精度/转义保留、缩进、行列、重复键、容量与层级。
- `npm run build`：Vue/TypeScript 类型检查和 Vite 生产构建通过。
- npm 安装审计：0 个已报告漏洞（本次检查时）。
- `git diff --check`：通过。

## Windows Edge 实际交互

通过 Playwright CLI 访问本地开发服务，验证：

- 示例自动格式化，大整数原文保留。
- 压缩单行输出；只校验清除旧输出。
- 尾逗号校验失败，错误定位聚焦输入编辑器。
- 4 空格缩进与 Ctrl+Enter。
- 复制后从剪贴板读回，内容包含完整大整数。
- 下载触发 result.json；浏览器报告已下载。
- 本机 package.json 文件导入成功。
- 清空、修改输入清除旧结果、菜单搜索无结果状态。
- 编辑器搜索面板、下一处匹配、换行开关。
- 菜单收起/展开、未知地址提示及返回。
- 刷新清空内容。

发现并修复：CodeMirror 默认 Ctrl+Enter 抢占自定义格式化快捷键，现使用最高优先级键位配置。

## 页面与生产构建

- 查看 1440px 桌面截图及 390px 窄屏长截图。
- 窄屏双栏变单栏，页面无横向溢出，编辑器和按钮可见。
- Windows 到 WSL 的 localhost:4173 转发本次未生效，使用 WSL 局域网地址验证生产预览。
- 生产构建的格式化、压缩及 Worker 加载通过。
- 捕获的生产请求仅为同源 GET 静态文件，无外部请求或数据提交请求；没有 pageerror。
- favicon 最初 404 已修复。

截图保存在本机项目 `output/playwright/`（被 Git 忽略）。开发用浏览器验证脚本也在该目录，部分含本机绝对路径，不作为跨机器测试入口。

## 未验证与边界

- 没有制作或测试 Windows 原生安装包。
- 未测试所有浏览器、DPI、原生文件权限与剪贴板拒绝场景。
- 5 秒超时分支和异步导入竞争未用故障注入测试；已实现终止/版本保护。
- 1 MiB 与 128 层限制有核心测试，不代表在所有设备上做过性能基准。
- 不含 CDN 实际发布、PWA 冷启动离线验证。

## 编码工具增量验证（2026-09-16）

### 自动检查

- npm ci 可复现安装通过，依赖审计为 0 个已报告漏洞。
- 新增 55 项编码测试，连同 JSON 的 29 项，总计 84 项全部通过。
- vue-tsc 类型检查、Vite 生产构建、git diff --check 通过。
- 覆盖 URL 三模式、Base64 字母表/填充/UTF-8、Unicode 代理对和单层规则、HTML 命名/数字/未知实体、空文本、BOM、容量边界。

### Windows Edge / Playwright

- 四个编码工具通过菜单切换，旧输入和结果清除，浏览器标题跟随工具更新。
- URL 参数往返、表单加号、非法百分号错误、设置变化清除结果通过。
- Base64 中文/emoji 往返、Base64URL、空白开关通过。
- 真实 CRLF 文件导入后编码为 YQ0KYg==，确认没有变成仅 LF。
- 剪贴板读回成功；下载文件名为 base64-encode.txt。
- Unicode 代理对往返、码点语法和 Ctrl+Enter 重复上次解码通过。
- HTML 解码后 img 标签仅为文本，没有生成 img 元素。
- 查看桌面 1440px 与窄屏 390px 截图；窄屏无页面横向溢出，横向菜单可切换工具。
- 刷新清空、原 JSON 示例格式化及大整数保留回归通过。
- 生产静态预览中四个页面的编码→结果作为输入→解码通过；17 个捕获请求均为同源 GET，没有 pageerror。

截图与本机验证脚本位于 output/playwright（Git 忽略）。本次未进行原生桌面包、5 秒超时故障注入或全浏览器兼容测试；不据此宣称这些已通过。

## JWT / 哈希 / HMAC / 时间戳 / SQL 增量验证（2026-09-16）

### 自动检查

- 新增 54 项核心测试，连同现有功能总计 138 项测试全部通过。
- vue-tsc 类型检查、Vite 生产构建、git diff --check 通过。
- 哈希与 HMAC 对照 Node crypto，覆盖四种算法、中文、空消息、长密钥和二进制输入；另有公开已知向量。
- JWT 覆盖分段错误、重复字段、UTF-8、NumericDate、过期边界及大整数保留。
- 时间戳覆盖负数、毫秒精度、显式偏移、闰年、纽约夏令时跳过与重复时间。
- SQL 覆盖注释、字符串、反引号标识符、占位符、多语句、格式选项及非法输入。

### Windows Edge / Playwright

- JWT 示例、未验签标签、时间解释和错误清除结果通过。
- MD5 / HMAC 已知向量、输出大小写、快捷键、设置变化清除旧结果通过。
- HMAC 默认隐藏密钥、显示/隐藏、空密钥显式选择、清空及切换页面清除密钥通过。
- 剪贴板读回、SQL 下载文件名、SQL 字符串保留及非法 SQL 提示通过。
- 时间戳双向转换、上海时区、纽约夏令时重复时间报错及显式偏移消歧通过。
- 查看 1440px 桌面及 390px 窄屏截图；修复时间页面触发的网格最小宽度溢出，时间戳和 HMAC 窄屏均无页面横向溢出。
- JSON 大整数保留与 Base64 示例回归通过，没有 pageerror。
- 生产静态预览再次验证五项新增功能及 JSON / Base64；33 个捕获请求均为同源 GET，没有请求失败或 pageerror，Worker 及其动态资源加载成功。

### 本次修复与边界

- Worker 动态导入需要 ES 输出，已配置 worker.format = 'es'。
- 开发首次加载动态依赖曾触发 Vite 重新优化和整页刷新；已显式配置 optimizeDeps，之后完整浏览器流程通过。
- 时间戳使用设备时间和运行环境时区数据；没有联网校时。
- JWT 仅解析，不执行验签。SQL 当前为 MySQL 方言，不连接数据库、不执行语句。
- 本次没有制作 Windows 安装包，没有执行 CDN 发布、全浏览器兼容测试或超时故障注入。
- 截图 hmac-desktop.png、timestamp-mobile.png 及验证脚本保存在 output/playwright/（Git 忽略）。

## 正则、二维码、TOTP 增量验证（2026-09-16）

- 41 项新增测试，合计 179 项通过；TOTP 覆盖 RFC 6238 的三种算法共 18 个标准向量，另有 Base32、URI、正则零宽匹配与容量边界测试。
- 类型检查、生产构建通过。
- Windows Edge 生产预览：正则匹配、灾难性回溯 2 秒超时终止及恢复通过；二维码图片加载和 PNG 下载通过；TOTP 示例、URI 导入、停止隐藏、清空、切页清除通过，无 pageerror。
- 二维码与 TOTP 的 390px 页面无横向溢出，已查看 TOTP 窄屏截图。
- 未进行真实手机扫码、真实账号登录、Windows 打包或所有浏览器兼容验证。截图与脚本在 output/playwright/。

## UUID、随机、文本与 HTTP 增量验证（2026-09-16）

- 新增 44 项测试，总计 223 项全部通过；覆盖版本/变体位、拒绝采样、字符集与数量限制、文本处理顺序、Diff 双侧重建、Unicode、换行、HTTP 查询与历史状态。
- Vue/TypeScript 类型检查、生产构建及 git diff --check 通过。
- Edge 生产页面验证：UUID 批量和下载、设置清除结果、随机字符范围与非法字符集提示、Diff 行/字符模式和 HTML 纯文本展示、文本处理及结果作为输入、HTTP 中文筛选和分类冲突通过。
- 生产验证捕获 16 个同源 GET 请求，无 pageerror，无外部数据请求。
- Diff 与 HTTP 在 390px 下无页面横向溢出；已查看 Diff 1440px 桌面截图。
- 未做随机分布统计认证、极限设备性能基准、文本 Worker 超时故障注入或 Windows 原生打包。
- 脚本与截图位于 output/playwright/，不作为跨机器测试入口。

## JSON / PHP / Python 增量验证（2026-09-16）

- 新增 40 项测试，总计 263 项通过；类型检查与生产构建通过。
- Edge 生产页面六个转换方向、大整数保留、键冲突提示、PHP 空数组策略、Ctrl+Enter、结果反向转换、converted.py 下载通过，无 pageerror。
- 390px 页面无横向溢出，截图 literal-mobile.png 位于 output/playwright/。
- 不执行 PHP/Python；未做完整语言语法兼容、真实运行时全面对照或超时故障注入。

## 图片 Base64 与二维码识别（2026-09-16）

- 新增 28 项测试，总计 291 项通过；类型检查、生产构建通过。
- Edge 生产预览验证图片上传、编码格式切换、二进制完全一致的往返、PNG 下载、MIME 冲突清除旧预览、二维码识别、SVG 拒绝、粘贴事件、无二维码提示、示例和清空，无 pageerror。
- 390px 两页无横向溢出；已查看二维码识别桌面截图。截图 qr-reader-desktop.png 与 image-base64-mobile.png 位于 output/playwright/。
- 粘贴使用合成 ClipboardEvent；未验证所有系统截图软件、相机、超时故障注入或原生安装包。

## 2FA 二维码导入（2026-09-16）

- 新增 10 项测试，全项目 301 项通过；类型检查与生产构建通过。
- Edge 生产页面验证 TOTP 参数导入、验证码启动、密钥隐藏、普通二维码拒绝并停止旧验证码、纯密钥默认参数、清空与切页清除通过，无 pageerror。
- 390px 无横向溢出，截图 totp-import-mobile.png 位于 output/playwright/。
- 未验证真实手机截图、相机或超时故障注入；清空流程测试不代表所有异步竞争时序均已测试。

## JSON 结构化对比（2026-09-16）

- 新增 26 项测试，全项目 327 项通过；类型检查和生产构建通过。
- Edge 生产预览验证原文红色装饰、缺少路径提示、点击列表、上一处/下一处、双侧定位且原文不变、编辑清除所有标记、无序相同、重复键拒绝、大整数不同、下载和旧 JSON 页面回归，无 pageerror。
- 390px 无横向溢出；已查看桌面截图。截图与脚本在 output/playwright/。
- 未验证全浏览器、超时故障注入或最大输入性能。

## RSA 工具验证

- 新增 20 项 RSA 自动化测试；全项目 10 个测试文件、347 项测试通过。
- Node/OpenSSL 交叉验证三种填充、两种密文编码；覆盖 2048 / 3072 / 4096 位生成、PEM 格式兼容、中文与空明文、长度边界和错误密钥等。
- npm run build 通过 Vue/TypeScript 检查与生产构建，产物包含独立 RSA Worker；git diff --check 通过。
- node-forge 1.4.0 及类型包安装后 npm audit 报告 0 个漏洞。
- 浏览器交互未完成：WSL Chromium 缺少 libnspr4.so，Windows 浏览器工具的沙箱初始化失败。仍需手动检查生成→加密→结果作为输入→解密、参数变更清空结果、取消任务、复制下载及窄屏布局。

## 图片压缩与格式转换（2026-09-17）
- Vitest：361 项通过，包含新增 14 项压缩输入/输出策略测试。
- 生产构建：WASM 与模块 Worker 资源正确打包。
- Windows Chrome：开发环境 16 组合、生产环境 48 组合通过；PNG/JPG/WebP 输入覆盖四档和四目标。
- 校验原格式保真像素/字节一致性、保真 PNG/WebP 转换的解码像素一致性、透明度与 JPG 白底。
- 下载、错误文件、动画拒绝、选项变更、取消、60 秒超时、粘贴、离页清理通过。
- 桌面与 390px 窄屏截图检查通过；正常处理无页面错误或外部上传请求。
- 未对用户真实素材及 TinyPNG 输出做基准对比；未验证 Safari/Firefox、桌面壳或专业广色域素材。
- 具体参数、元数据处理和第三方许可见 IMAGE_COMPRESSION.md。

## ZIP 图片批量处理（2026-09-17）
- 新增 26 项 ZIP 策略/引擎测试，全项目 387 项通过。
- 生产构建与 Windows Chrome 原格式/WebP ZIP 处理、下载、错误包拒绝、单图回归通过。
- Python zipfile 独立校验下载 ZIP 的 CRC、目录、重名与非处理文件原始字节，均通过。
- 取消、修改选项、单图 60 秒超时回退、整包 120 秒阶段超时、换文件和离页清理通过。
- 桌面/390px 窄屏截图检查通过；完整范围与未验证项见 ZIP_IMAGE_PROCESSING.md。
