# 图片加水印设计验收

final result: passed

## 范围与证据
- 目标：用户确认“按这个功能实现”的演示图，并将颜色预设改为黑、白、红、黄，增加色号及英文颜色名。沿用现有 DevKit 壳层，不要求复制演示素材本身。
- 参考图：C:/Users/Administrator/.codex/generated_images/01a0af95-1552-78c1-8638-63d308b1099c/exec-0600b5ac-a851-4c8c-8482-532d66040cd0.png
- 实现地址：http://localhost:5191/#/watermark
- 最终截图：C:/Users/Administrator/.codex/visualizations/2026/09/17/01a0af95-1552-78c1-8638-63d308b1099c/watermark-final-desktop.png
- 窄屏截图：同目录 watermark-final-mobile.png。
- 桌面 viewport 1536×1024 CSS px，截图同尺寸，1×；参考图 1536×1024。另以 390×844 CSS px 检查窄屏。
- 对比状态：有本地图片、选中文字水印、参数区、底部导出区，未生成。参考图有两个水印；最终首屏使用单水印，复制后已验证对象选择器和独立参数。示例图片是浏览器生成的测试素材，不是产品附带资产。
- 已将参考图及初版截图并排缩略比较整体结构；最终桌面和窄屏截图分别打开检查。局部检查覆盖颜色输入、选中边框、旋转文字及导出按钮。

## 检查与修复
- P2：初版设置区较高，主要导出按钮落在首屏外。减少表单间距和文本框高度；单个水印时隐藏冗余选择器。最终生成按钮 y=955.47、height=38，在 1024 px 首屏内。
- 字体与层级：沿用项目字体、标题、说明和按钮样式。长文件名换行，输入文字和设置标签均可读。
- 间距布局：左预览、右设置、下导出，340 px 设置列；窄屏改为单列，没有页面水平溢出。多个水印、错误提示或较低窗口高度允许自然纵向滚动。
- 色彩：沿用紫色操作色，黑白红黄预设和自定义色符合修订要求。白色色块有边框，选中状态有紫色外圈。
- 图像：实际本地上传内容；预览不拉伸，竖图比例实测通过。选中框只出现在预览；拖动与导出使用同一坐标系统。
- 文案：区分“复制此水印”和“复制图片”；说明透明度、超界裁切、PNG 剪贴板与下载格式的区别。
- 无剩余阻断性 P0/P1/P2 项。未将参考图的示例证件、虚构导航或光影效果当作真实功能资产。

## 功能证据
- Windows Chrome 开发及生产构建：水印复制/删除、拖动、颜色校验、三种格式×四档压缩、下载和实际剪贴板回读通过，无页面异常或外部上传请求。
- PNG / 保真 WebP 解码后 SHA-256 像素一致，噪声样图上确认体积增大仍保留无损结果。
- 模拟压缩候选更大，确认回退图像逐像素保持带水印基准。
- 493 项单元测试、生产构建、git diff --check 通过。
- 待其他环境手工验收：Safari/Firefox、真实移动端触控、桌面壳。窄屏检查为 Windows Chrome viewport 模拟。
